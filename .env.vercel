# =============================================================================
# VERCEL ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================
# Copy these variables to your Vercel project settings
# Dashboard -> Project -> Settings -> Environment Variables

# =============================================================================
# REQUIRED FOR BUILD & RUNTIME
# =============================================================================

# Supabase (REQUIRED - These are causing your build error)
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# NextAuth (REQUIRED)
NEXTAUTH_URL=https://your-vercel-app.vercel.app
NEXTAUTH_SECRET=your-nextauth-secret-32-chars-min

# Google OAuth (REQUIRED for YouTube integration)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
YOUTUBE_API_KEY=AIza...your-youtube-api-key

# OpenAI (REQUIRED for AI responses)
OPENAI_API_KEY=sk-...your-openai-api-key

# =============================================================================
# OPTIONAL SERVICES (Set to empty string if not using)
# =============================================================================

# Embedding Provider (optional - defaults to 'deepinfra')
EMBEDDING_PROVIDER=deepinfra
DEEPINFRA_API_KEY=your-deepinfra-key-or-leave-empty


# ElevenLabs (optional - only if using voice features)
ELEVENLABS_API_KEY=your-elevenlabs-key-or-leave-empty

# LiveKit (optional - only if using real-time voice)
LIVEKIT_URL=wss://your-livekit-url
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# Hugging Face (optional - for sentiment analysis)
HF_TOKEN=hf_...your-hugging-face-token

# =============================================================================
# VERCEL DEPLOYMENT INSTRUCTIONS
# =============================================================================
#
# 1. Go to your Vercel Dashboard
# 2. Select your project -> Settings -> Environment Variables
# 3. Add each variable above with its value
# 4. Make sure to set them for all environments (Production, Preview, Development)
# 5. The most critical ones that MUST be set are:
#    - SUPABASE_URL
#    - SUPABASE_SERVICE_ROLE_KEY
#    - NEXTAUTH_URL
#    - NEXTAUTH_SECRET
#    - GOOGLE_CLIENT_ID
#    - GOOGLE_CLIENT_SECRET
#    - YOUTUBE_API_KEY
#    - OPENAI_API_KEY
#
# =============================================================================